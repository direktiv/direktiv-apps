name: googleinvoke
on: 
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'     
        required: true
        default: 'warning'
      tags:
        description: 'Test scenario tags'  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.15
    - name: Create-AppDir
      run: mkdir google
    - name: Move-Dockerfile
      run: cp googleinvoke/Dockerfile ./google/
    - name: Move-google-Readme
      run: cp googleinvoke/README.md ./google/     
    - name: Find-Direktivapps
      run: go get github.com/vorteil/direktiv-apps/pkg/direktivapps
    - name: Find-AWS-Dependencies
      run: go get golang.org/x/oauth2 golang.org/x/oauth2/jwt 
    - name: Build
      run: CGO_ENABLED=0 go build -o ./google/google googleinvoke/main.go
    - uses: actions/upload-artifact@v2
      with:
        name: google
        path: google

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Download google Application
      uses: actions/download-artifact@v2
      with:
        name: google
        path: google


    - name: chmod +x google binary
      run: sudo chmod +x ./google/google    
    - name: Setup Docker Buildx
      uses: docker/setup-buildx-action@v1

    - name: Login to Dockerhub
      uses: docker/login-action@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}  
    
    # - name: Add SHORT_SHA env property with commit short sha
    #   run: echo "SHORT_SHA=`echo ${GITHUB_SHA} | cut -c1-8`" >> $GITHUB_ENV
    # - name: Get the version
    #   id: get_version
    #   run: echo ::set-output name=VERSION::$(echo $GITHUB_REF | cut -d / -f 3)

    - name: Build and Push
      id: docker_build
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: vorteil/googleinvoke:latest
        context: .
        file: ./google/Dockerfile
        platforms: linux/amd64

    - name: Build and Push
      id: docker_build2
      uses: docker/build-push-action@v2
      with:
        push: true
        tags: vorteil/googleinvoke:${{ github.event.inputs.tags }}
        context: .
        file: ./google/Dockerfile
        platforms: linux/amd64

    - name: Sync
      uses: ms-jpq/sync-dockerhub-readme@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
        repository: vorteil/googleinvoke
        readme: "./google/README.md"